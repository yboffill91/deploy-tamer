version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # 🔑 Confirmar que la CLERK_SECRET_KEY está disponible
        - 'if [ -z "$CLERK_SECRET_KEY" ]; then echo "❌ CLERK_SECRET_KEY MISSING"; exit 1; else echo "✅ CLERK_SECRET_KEY FOUND"; fi'
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
